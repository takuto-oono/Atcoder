package main
import (
	"fmt"
)

func main(){
	var h, w, x, y , ans int
	fmt.Scan(&h, &w, &x, &y)
	ans = 1

	P = [][] string {}

	var s string

	for i := 0; i < h; i ++ {
		fmt.Scan(&s)
		S = [] string {}
		for j := 0; j < w; j ++ {
			S = append(S, s[j])
		}
		P = append(P, S)
	}

	for i := x
}